Hi <%= @user.name %>,

We received a request to reset your password. Use the token below with POST /api/v1/auth/change_password (field auth.reset_token):

<%= @token %>

If you did not request a password reset, you can safely ignore this email.

Best regards,
Job Vacancy Manager
