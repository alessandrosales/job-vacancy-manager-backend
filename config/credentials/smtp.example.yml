# Copie o conteúdo abaixo para o arquivo criptografado:
#   bin/rails credentials:edit
#
# (Remova estes comentários iniciados com # no editor de credentials — YAML não aceita # dentro do valor.)

# smtp:
#   address: smtp.sendgrid.net
#   port: 587
#   domain: seu-dominio.com
#   user_name: apikey
#   password: SECRET
#   authentication: plain
#   enable_starttls_auto: true
#   ssl: false
#
# mailer:
#   default_from: "Job Vacancy <no-reply@seudominio.com>"
#   default_url_host: api.seudominio.com
#   default_url_protocol: https
