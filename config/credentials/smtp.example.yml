# Mailer e SMTP passam a ser configurados por variáveis de ambiente.
# Veja o template na raiz do projeto: .env.example
#
# Desenvolvimento: copie .env.example para .env e ajuste.
# Produção / Kamal: defina as mesmas chaves em config/deploy.yml (env:) ou no provedor.
